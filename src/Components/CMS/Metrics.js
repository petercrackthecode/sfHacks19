import React, {Component} from 'react';

export default class Metrics extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return(
            <div>
                Metrics
            </div>
        );
    }
}
